# NoSQL - MongoDB

## Description
This project introduces NoSQL databases, document storage, and CRUD operations using MongoDB shell and PyMongo in Python.

## Requirements
- All MongoDB command files start with a comment `// comment` and end with a new line.
- Python scripts follow `pycodestyle` and Python 3.9 standards.
